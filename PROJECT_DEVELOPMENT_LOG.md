# Cross100 & Cross Solver — 完整开发日志

> 本文档记录了 cross100（色彩消除益智游戏）和 cross-solver（独立求解器）项目从零到发布的全部开发过程，包含每一次 Bug 修复、每一个设计决策、每一轮用户反馈的详细内容。

---

## 目录

- [第一阶段：游戏核心开发（2026年5月下旬，具体日期不详）](#第一阶段游戏核心开发2026年5月下旬具体日期不详)
- [第二阶段：Gateway 中断排查与求解器研究（2026年5月27日–28日）](#第二阶段gateway-中断排查与求解器研究2026年5月27日28日)
- [第三阶段：环境检查与 README 更新（2026年5月27日）](#第三阶段环境检查与-readme-更新2026年5月27日)
- [第四阶段：PROJECT_SUMMARY.md 编写与20轮审查（2026年5月27日–30日）](#第四阶段project_summarymd-编写与20轮审查2026年5月27日30日)
- [第五阶段：v1.1.0 与 v1.2.0 对比分析（2026年5月30日）](#第五阶段v110-与-v120-对比分析2026年5月30日)
- [第六阶段：需求误解——独立求解器 APK（2026年5月30日）](#第六阶段需求误解独立求解器-apk2026年5月30日)
- [第七阶段：求解器 APK 开发（2026年5月30日）](#第七阶段求解器-apk-开发2026年5月30日)
- [第八阶段：APK 编译问题排查（2026年5月30日）](#第八阶段apk-编译问题排查2026年5月30日)
- [第九阶段：UI 与排序修复（2026年5月30日）](#第九阶段ui-与排序修复2026年5月30日)
- [附录 A：完整 Git 提交历史](#附录-a完整-git-提交历史)
- [附录 B：关键设计决策汇总](#附录-b关键设计决策汇总)
- [附录 C：所有已修复 Bug 清单](#附录-c所有已修复-bug-清单)

---

## 第一阶段：游戏核心开发（2026年5月下旬，具体日期不详）

### 概述

这是项目的诞生阶段。用户与 AI 协作，从零开始构建了一个完整的 HTML5 益智游戏。整个阶段共完成 **45 项操作**，游戏从最初代号 "prism2048" 最终更名为 "cross100"。

### 1.1 游戏 HTML 文件创建

**操作 #1：创建 game.html**

项目第一个文件 game.html 被创建，作为游戏的主入口文件。技术选型为 **HTML5 + WebView**，原因是这是在 Termux 环境下最简单可行的方案——不需要 Android Studio、Gradle 等重型工具链，只需要一个 HTML 文件和 Android SDK 的命令行工具即可打包成 APK。

### 1.2 核心游戏逻辑实现

**操作 #2：实现核心游戏逻辑**

实现了游戏的三大核心函数：
- `tap(row, col)`：点击一个灰色方块，触发消除
- `cascade()`：级联消除逻辑——点击一个灰色方块后，与之相邻（上下左右）的灰色方块也会被消除，形成连锁反应
- `checkWin()`：胜利检测——判断当前局面是否已经被完全清除

**游戏规则确定：**
- 棋盘为 10×10 网格
- 每次点击一个灰色方块，该方块及其相邻灰色方块会被消除
- 胜利条件：最后一次点击恰好消除剩余的 19 个灰色方块
- 失败条件：剩余灰色方块数量 < 19 且无法清除

### 1.3 主题切换功能

**操作 #3：添加主题切换**

实现了 Monument Valley（纪念碑谷）风格和 Pixel（像素）风格两种视觉主题的切换功能。用户可以通过按钮在两种风格之间切换。

### 1.4 冲击波动画

**操作 #4：实现冲击波动画**

添加了点击时的 shockwave（冲击波）视觉效果，增强游戏的交互反馈感。

**操作 #5：修复冲击波位置偏移**

发现冲击波动画的位置不准确。排查后发现是 `getBoundingClientRect()` 返回的坐标需要修正，以正确对齐点击位置。

**修复方法：** 对 getBoundingClientRect 的返回值进行校正计算。

### 1.5 关卡生成重写

**操作 #6：重写游戏逻辑——关卡生成方式**

将关卡生成逻辑从简单随机改为 **堆叠生成法（stacking generation method）**。核心思路：
1. 先生成一个"解法序列"（一系列点击位置）
2. 按照解法序列的逆序，逐步在棋盘上"堆叠"灰色方块
3. 玩家需要找到原始的解法序列来消除所有方块

这种方式确保每个生成的关卡都有解，且解法就是生成过程的逆序。

### 1.6 Crack（解法演示）功能

**操作 #7：实现 crack 功能——解法动画播放**

添加了 crack 功能：点击 crack 按钮后，游戏会自动播放解法动画，一步一步展示如何消除所有灰色方块。这相当于一个"解谜提示"功能。

**操作 #8：修复 crack 模式的 busy 检查**

发现 crack 动画播放期间，用户如果再次点击 crack 按钮会导致冲突。添加了 `cracking` 标志位来防止重复触发。

### 1.7 失败检测

**操作 #9：实现失败检测——checkStuck 函数**

实现了 `checkStuck()` 函数来检测游戏是否陷入死局（无法继续消除所有方块）。

**操作 #10：修复 checkStuck 逻辑**

初始实现有误——需要遍历所有灰色方块来判断是否存在可行的操作，而不是只检查部分方块。

**操作 #11：简化 checkStuck**

最终将失败检测简化为：当剩余灰色方块数量 < 19 时，直接判定为失败。因为每次点击最多消除 19 个方块（中心方块 + 最多 18 个相邻方块的级联），如果剩余不足 19 个，最后一次点击无法一次性清除所有方块。

### 1.8 严重 Bug：看门狗语法错误

**操作 #12：修复看门狗语法错误**

这是一个**致命 Bug**。代码中出现了 `var try{...}` 这样的非法语法，导致整个 JavaScript 脚本解析失败，页面完全白屏。

**根因分析：** `var try{...}` 中的 `var` 关键字与 `try` 语句块冲突，JavaScript 引擎无法解析这段代码，导致整个 `<script>` 标签内的所有代码都不执行。

**修复方法：** 移除非法的 `var` 关键字。

### 1.9 CSS 兼容性问题

**操作 #13：修复 CSS 属性选择器不兼容**

这是一个重要的兼容性发现：**Android WebView 不支持 CSS 属性选择器**（如 `[data-color="1"]`）。

**现象：** 使用 CSS 属性选择器设置方块颜色的样式完全不生效，方块显示为默认颜色。

**修复方法：** 放弃 CSS 属性选择器，改为在 JavaScript 中通过 `element.style.xxx = value` 直接设置内联样式。

### 1.10 Crack 模式误报

**操作 #14：修复 crack 播放期间的失败误报**

发现 crack 动画播放过程中，`checkStuck()` 会误判为失败（因为动画播放过程中棋盘状态是变化的）。

**修复方法：** 在 `checkStuck()` 中添加 `!cracking` 条件判断，crack 播放期间跳过失败检测。

### 1.11 失败界面按钮

**操作 #15：添加 RESTART/NEW GAME 按钮到失败覆盖层**

在游戏失败的覆盖层（overlay）上添加了两个按钮：
- **RESTART**：重新开始当前关卡（使用相同的种子）
- **NEW GAME**：生成一个全新的关卡

### 1.12 Crack 中断功能

**操作 #16：实现 crack 中断——crackStop 标志**

添加了 `crackStop` 标志位，允许用户在 crack 动画播放过程中中断播放。

### 1.13 精简统计信息

**操作 #17：移除 Level 和 Best 统计，只保留 Taps/Left**

简化了游戏界面上的统计信息显示，移除了关卡数（Level）和最佳成绩（Best），只保留点击次数（Taps）和剩余方块数（Left）。

### 1.14 项目更名

**操作 #18：项目更名 prism2048 → cross100**

将项目从最初的代号 "prism2048" 更名为 "cross100"。"cross" 反映了游戏的十字消除机制，"100" 对应 10×10 的棋盘。

### 1.15 UI 布局重组

**操作 #19：重组 UI 按钮布局——X/◇/? 排列**

重新设计了游戏界面的按钮布局，采用 X（关闭/重启）、◇（解法演示）、?（帮助）三个按钮的排列方式。

**操作 #20：添加帮助覆盖层**

添加了帮助弹窗（help overlay），向玩家展示游戏规则和操作说明。

### 1.16 按钮状态管理

**操作 #21：添加 tapped 标志——首次点击后禁用 X 按钮**

添加了 `tapped` 标志位。玩家首次点击方块后，X 按钮被禁用，防止在游戏进行中误操作重新开始。

**操作 #22：更新 demo 播放时 X 按钮的图标**

在 crack 解法演示播放期间，X 按钮的图标从 ✕ 变为 ▶（播放图标），表示此时点击可以中断播放。

### 1.17 覆盖层关闭交互

**操作 #23：修复帮助覆盖层关闭方式**

初始实现中帮助覆盖层需要点击特定关闭按钮才能关闭。修改为：**点击覆盖层任意位置即可关闭**。

### 1.18 图标对齐修复

**操作 #24：修复 ◇/▶ 图标的对齐问题**

发现切换按钮在不同状态下的图标（◇ 和 ▶）存在 CSS 偏移对齐问题。通过 CSS offset 校正修复。

**操作 #25：修复 CSS 位置错误**

发现部分样式代码被错误地放在了不正确的位置（可能放在了 JavaScript 代码块中）。将样式代码移回 `<style>` 标签内。

### 1.19 完整重建

**操作 #26：从备份完整重建 game.html**

由于多次修改导致代码中出现"行号污染"（修改注释和实际代码行号不对应，难以维护），决定从备份完整重建 game.html 文件。

### 1.20 SVG 图标替换

**操作 #27：SVG 图标替换 Unicode 字符**

将游戏界面中使用的 Unicode 特殊字符（如 ✕、◇、▶、↩ 等）替换为 SVG 矢量图形，以获得更一致、更清晰的视觉效果。

### 1.21 Crack 期间按钮禁用

**操作 #28：修复 crack 期间按钮的可点击状态**

发现 crack 动画播放期间，界面按钮仍然可以被点击，可能导致状态冲突。修复为：crack 播放期间所有操作按钮均被禁用。

### 1.22 按钮样式调整

**操作 #29：修复按钮背景透明度**

发现圆形按钮的背景色太浅，在某些主题下不够醒目。将按钮背景的透明度调深。

**操作 #30：修复主题切换后的渲染问题**

发现切换主题后，棋盘上的方块颜色没有立即更新。修复方法：在主题切换后调用 `render()` 函数重新渲染整个棋盘。

### 1.23 GitHub 仓库创建与发布

**操作 #31：创建 GitHub 仓库**

创建了 GitHub 仓库：https://github.com/Pokem0n2/cross100

**操作 #32：构建 APK**

使用手动 Android SDK 工具链构建了第一个 APK 文件，大小仅 21KB。

构建流程（后续详细记录在 README 中）：
1. `aapt2 compile` 编译资源
2. `aapt2 link` 链接资源和 android.jar
3. `javac` 编译 Java 代码
4. `d8` 将 .class 转为 DEX
5. 将 DEX 和 assets 注入 APK
6. `apksigner` 签名

**操作 #33：上传 GitHub Release v1.0.0**

将 v1.0.0 版本的 APK 上传到 GitHub Release。

### 1.24 胜利检测时序 Bug（关键修复）

**操作 #34：修复胜利检测时序 Bug**

这是一个**关键 Bug**，导致胜利画面无法正确显示。

**根因分析：** 游戏的棋盘数据更新（消除方块）被放在了一个异步的 `setTimeout` 回调中，但胜利/失败检测代码在 `setTimeout` 之前就同步执行了。这意味着检测函数读取到的是旧的棋盘数据，而不是更新后的数据。

**修复方法：** 将棋盘数据更新（grid 更新和 grayClr 计数）改为同步执行，胜利/失败检测不再依赖异步动画回调。

**操作 #35：修复按钮动作路由**

发现失败覆盖层上的按钮点击后执行的动作不正确：
- 胜利覆盖层的按钮应该触发 `newGame()`（新游戏）
- 失败覆盖层的按钮应该触发 `restartLevel()`（重玩当前关卡）

### 1.25 胜利界面

**操作 #36：添加胜利覆盖层**

添加了 "CLEARED!" 祝贺弹窗，配合 NEW GAME 按钮，让玩家在通关后可以选择开始新游戏。

### 1.26 撤销功能

**操作 #37：添加撤销按钮**

添加了 ↩ 撤销图标按钮，定位在 NEW GAME 按钮上方 50px 处，纯图标无背景样式。

**操作 #38：实现撤销逻辑**

实现了完整的撤销系统：
- `undoStack` 数组存储棋盘快照（每次点击前保存当前 grid 的深拷贝）
- 支持无限撤销，可以一直回退到初始状态
- 撤销时从 undoStack 弹出最后一个快照，恢复到 grid 中

### 1.27 种子系统

**操作 #39：添加种子显示**

在界面上添加了 "seed：xxxxxxxx" 文本标签，显示当前关卡的种子值。最初使用 FNV-1a 哈希算法生成种子。

**操作 #40：重构种子为 PRNG——mulberry32**

将种子机制从 FNV-1a 哈希重构为 **mulberry32 伪随机数生成器（PRNG）**：
- 使用 32 位整数作为种子
- mulberry32 算法确保相同的种子总是生成相同的关卡（确定性）
- 这使得分享种子 = 分享关卡

**操作 #41：添加复制种子按钮**

添加了 "Copy Seed" 按钮，点击后将当前种子复制到剪贴板，方便分享。

**操作 #42：添加加载种子按钮**

添加了 "Load Seed" 按钮，点击后弹出输入框，玩家可以输入种子值来加载特定关卡。

**操作 #43：样式化加载种子覆盖层**

将加载种子的弹窗样式设计为与帮助弹窗一致的风格，并适配了 `visualViewport` 事件来处理键盘弹出时的布局调整。

### 1.28 棋盘布局调整

**操作 #44：调整棋盘边距**

将棋盘整体上移了 55px，优化在不同屏幕尺寸下的显示效果。

### 1.29 导入网格功能

**操作 #45：添加导入网格功能**

添加了导入网格功能——玩家可以粘贴一个 10×10 的数字矩阵来加载旧关卡。这是为了兼容旧版本（使用 `Math.random()` 生成的关卡无法通过 mulberry32 种子重现）。

### Phase 1 关键设计决策总结

| 决策 | 选择 | 原因 |
|------|------|------|
| 技术栈 | HTML5 + WebView | Termux 环境下最简单的可行方案 |
| 胜利条件 | 最后一次点击恰好消除 19 个方块 | 确保解法的精确性 |
| 关卡生成 | 堆叠法，解法 = 生成序列的逆序 | 保证每关必有解 |
| 失败检测 | 剩余 < 19 即失败 | 简化逻辑，减少误判 |
| CSS 方案 | 内联样式（JS 设置） | WebView 不支持 CSS 属性选择器 |
| 品牌名 | Cross100 | 反映十字消除机制和 10×10 棋盘 |
| 检测时机 | 同步执行 | 避免异步回调导致的数据不一致 |
| 种子算法 | mulberry32 PRNG | 32 位整数种子，确定性可复现 |

### Phase 1 关键 Bug 清单

| Bug | 现象 | 根因 | 修复 |
|-----|------|------|------|
| FAILED 不触发 | 失败覆盖层不显示 | `var try{...}` 非法语法导致整个脚本崩溃 | 移除 `var` 关键字 |
| 方块不显示 | 棋盘上看不到彩色方块 | WebView 不支持 CSS 属性选择器 | 改用 JS 内联样式 |
| Crack 误报失败 | 播放解法动画时弹出失败提示 | crack 期间 checkStuck 仍执行 | 添加 `!cracking` 条件 |
| 胜利画面不出现 | 通关后无祝贺弹窗 | 棋盘更新在异步回调中，检测在同步中先执行 | 改为同步更新 |
| 旧关卡种子搜索失败 | 搜索所有 42 亿种子无匹配 | 旧版使用 Math.random()，新版用 mulberry32 | 提供导入网格功能 |
| 白屏（全页空白） | 页面完全不显示 | stuckBanner innerHTML 中的单引号与 JS 字符串引号冲突，导致整个 script 块解析失败 | 修复引号转义 |

---

## 第二阶段：Gateway 中断排查与求解器研究（2026年5月27日–28日）

### 2.1 Hermes Gateway 中断问题

**用户反馈：** 用户报告 hermes gateway run 会话总是中断。

**排查过程：**
- `hermes gateway run` 是一个前台进程
- 在 Android 系统中，前台进程容易被以下情况杀死：
  - Android OOM Killer（内存不足时系统自动杀进程）
  - 终端关闭
  - 网络断开

**解决方案：** 使用后台服务模式：
```bash
hermes gateway install    # 安装为系统服务
hermes gateway start      # 以后台服务方式启动
```

这样 gateway 就不会因为终端关闭或网络断开而中断。

### 2.2 旧关卡的18步解法

用户提供了旧版本（使用 Math.random() 生成）的一个关卡的18步解法序列：

```
(1,1) → (1,9) → (1,9) → (5,5) → (6,4) → (6,5) → (7,1) → (7,4)
→ (8,4) → (8,9) → (9,2) → (9,4) → (10,4) → (10,5) → (10,5)
→ (10,7) → (10,9) → (4,9)
```

经过搜索所有 4,294,967,296 个 mulberry32 种子，确认无法复现此关卡。原因是旧版本使用的是 `Math.random()`（非确定性），而新版本使用的是 mulberry32 PRNG（确定性）。两种算法的随机序列完全不同，因此旧关卡无法通过新种子系统复现。

**结论：** 必须通过"导入网格"功能来加载旧关卡，种子系统只适用于新版本生成的关卡。

---

## 第三阶段：环境检查与 README 更新（2026年5月27日）

### 3.1 Harness 工程环境验证

用户要求检查当前的 Android APK 构建环境。逐一验证了各个工具的版本：

| 工具 | 版本 | 说明 |
|------|------|------|
| javac | 21.0.11 | OpenJDK 21，Java 编译器 |
| aapt2 | 2.19 | Android Asset Packaging Tool 2 |
| d8 | 3.3.20-dev | DEX 编译器，将 .class 转为 .dex |
| apksigner | 0.9 | APK 签名工具 |
| android.jar | android-34 | 目标平台 Android 34 (API 34) |

**关键路径：** `/data/data/com.termux/files/home/android-sdk/platforms/android-34/android-34/android.jar`

### 3.2 README.md 更新

更新了 README.md，添加了：
- 完整的功能列表
- APK 构建步骤（使用命令行 Android SDK 工具链）
- 工具版本信息

**提交：** `27c3fa2` — docs: add APK toolchain versions and build steps to README

---

## 第四阶段：PROJECT_SUMMARY.md 编写与20轮审查（2026年5月27日–30日）

### 4.1 用户需求

用户提出："编辑一份细节完整的cross100项目总结，对总结进行20轮检查是否有遗漏需要补充"

要求：创建一份超过 850 行的完整技术参考文档，涵盖项目的所有方面，并进行 20 轮审查确保零遗漏。

### 4.2 初始版本

创建了 PROJECT_SUMMARY.md，初始版本 825 行，包含 16 个章节。

**提交：** `28269b9` — docs: add comprehensive PROJECT_SUMMARY.md (16 sections, 825 lines)

### 4.3 第1轮审查（发现 35 个问题）

这是发现问题最多的一轮，共发现 35 个问题：

**i18n 相关（3个）：**
1. i18n 密钥数量错误：文档写的 28 个，实际是 34 个
2. i18n 对照表不完整：只列出了 11/34 个密钥
3. 需要补充完整的 i18n 密钥对照表

**主题样式相关（4个）：**
4. Valley 主题字体栈缺少 `-apple-system`
5. Valley 主题缺少 `--sh` CSS 变量
6. Pixel 主题缺少 7 个 CSS 变量
7. 粒子重力效果描述为 Pixel 主题专属（实际上适用于所有主题）

**求解器代码相关（3个）：**
8. 求解器代码中使用了 `Math.floor` 而非 `Math.round`
9. 求解器 taps 对象属性描述错误
10. 求解器贪心调度代码过于简化/不准确

**HTML 结构相关（3个）：**
11. HTML 结构树缺少 header/footer 包装器
12. HTML 结构缺少多个元素和属性
13. 覆盖层关闭行为描述不正确

**缺失内容（6个）：**
14. 缺少事件监听器文档
15. 缺少 SVG 常量文档
16. 缺少 DOM 引用常量文档
17. 缺少 CV/CP 渲染数组文档
18. 缺少 meta 标签文档
19. gen() 函数回退到 Math.random 的逻辑未记录

**代码标注（2个）：**
20. showStuck() 未标记为死代码
21. 缺少 crackStop 检查点文档

**功能遗漏（1个）：**
22. crack() 函数无解时的 alert 提示未记录

### 4.4 第2轮审查（发现 6 个问题，修复 4 个）

1. 求解器 Z 验证缺少空单元格 `z===0` 的检查
2. 求解器贪心调度使用了 for 循环而非 while+Set+retry 模式
3. showStuck() 死代码标注（已添加）
4. 帮助覆盖层与种子/导入覆盖层的关闭行为差异说明

### 4.5 第3轮审查（发现 16 个问题）

**数值错误：**
1. `#rm` 初始值错误：文档写的 0，实际是 100

**元素类型错误：**
2. `#ov` 覆盖层元素类型错误：文档写的 div，实际是 p
3. 元素顺序描述错误
4. 缺少 `<strong>` 标签

**样式类缺失：**
5. Seed 按钮缺少 `btn` class 和 `data-i18n` 属性
6. Seed/导入覆盖层缺少 `h3`、`p`、`data-i18n` 属性

**图标显示错误：**
7. `#bu` 撤销按钮文档写的显示文本 "Undo"，实际显示的是 SVG 图标

**属性缺失：**
8. 多个 HTML 元素缺少属性文档

### 4.6 第4–8轮审查（行为准确性，0个问题）

连续5轮审查，验证了 8 个行为项，全部正确：

1. ✅ 19 次递减逻辑正确
2. ✅ 胜利/失败条件描述准确
3. ✅ gen() 交集逻辑正确
4. ✅ 粒子效果行为正确
5. ✅ 冲击波效果行为正确
6. ✅ crack 定时逻辑正确
7. ✅ 看门狗行为正确
8. ✅ 点击动画行为正确

### 4.7 第9–14轮审查（交叉一致性，发现 3 个问题）

1. 贪心调度代码与实际源码不一致
2. Meta 标签章节被错误归属于 walkthrough 文件，实际来自 index.html
3. 帮助覆盖层关闭行为的解释有误导性

### 4.8 第15–20轮审查（最终检查）

**发现 1 个问题：**
- Walkthrough 文件大小：文档写的 831KB，实际是 812KB

**全部修复完成。**

### 4.9 审查阶段的 Git 提交

| 提交 | 内容 |
|------|------|
| `28269b9` | 初始版本，825 行 |
| `41624f3` | 第二轮：求解器精度、死代码标注 |
| `bb8ce5e` | 第三轮：HTML 结构、覆盖层细节 |
| `865acb3` | 第4–20轮：求解器代码精度、meta 标签归属、文件大小修正 |

**总修复数：55+ 处，跨越 20 轮审查，4 次提交。**

---

## 第五阶段：v1.1.0 与 v1.2.0 对比分析（2026年5月30日）

### 5.1 用户反馈

用户安装了 v1.1.0 和 v1.2.0 两个版本的 APK，感觉**两者没有区别**。

### 5.2 版本分析

**v1.1.0（提交 b62cea5，2026-05-29）：**

这是一个**存在问题的发布版本**。虽然代码仓库中已经包含了 i18n、seed、undo、import、solver 等功能的代码，但 APK 中的 assets 目录下的 index.html 文件是**过时的旧版本**，仍然只包含基础游戏和主题切换功能。

这意味着 v1.1.0 的 APK 虽然打了 tag，但实际打包的 HTML 文件并不是最新代码。

**v1.2.0（提交 10c0b5e，2026-05-30）：**

这个版本将 `apk/assets/index.html` 完整同步到了最新版本。两个 tag 之间有 3 个提交：
1. 通用求解器功能（universal solver）
2. 求解器修复
3. Walkthrough 更新

新增功能：i18n 中英文切换、种子系统、撤销、导入网格、PRNG、universalSolver。

### 5.3 用户无感知的原因

1. **求解器是隐藏的** — 只有当 `sol[]` 为空时（即导入的网格没有预计算解法）才会触发 universalSolver
2. **i18n 默认语言从中文改为英文** — 但如果用户使用中文界面，视觉上没有变化
3. **Seed/Undo/Import 是辅助功能** — 不影响核心游戏玩法

### 5.4 教训

APK 发布前必须确认 assets 目录中的文件是最新的。v1.1.0 的根本问题是打包时使用了过时的 index.html 文件。

---

## 第六阶段：需求误解——独立求解器 APK（2026年5月30日）

### 6.1 用户原始需求

用户提出："用相同的编程语言和UI制作一个单独的求解器apk"

### 6.2 AI 的误解

AI 将此理解为：在游戏的 index.html 中嵌入求解器代码，作为 `crack()` 函数的内部功能。这实际上是在游戏内添加了一个解法演示功能。

### 6.3 用户纠正

用户明确指出："求解器不是播放动画的形式，而是文字的形式逐行显示步骤"

### 6.4 用户真正想要的是

一个**完全独立的 APP**：
- 独立的包名：`com.cross100.solver`
- 独立的 APK 文件
- 文字形式的步骤输出："Step 01, R 5 C 10"
- 不是动画播放，只是一个步骤列表
- 与游戏相同的编程语言（HTML5 + WebView）和 UI 风格

### 6.5 教训

在接到模糊需求时，应该先向用户确认具体预期，而不是自行假设。"用相同的编程语言和UI"指的是技术栈和视觉风格，而不是将功能嵌入现有 APP。

---

## 第七阶段：求解器 APK 开发（2026年5月30日）

### 7.1 创建 solver.html

创建了 `solver/solver.html`，文件大小约 15KB，功能包括：

**视觉设计：**
- 双主题支持（Valley/Pixel），与游戏风格一致
- EN/CN 语言切换

**输入方式：**
- 粘贴矩阵：用户可以粘贴一个 10×10 的数字矩阵
- 加载种子：输入种子值，使用与游戏相同的 mulberry32 PRNG 生成关卡

**求解算法：**
- universalSolver 算法（与游戏内版本相同）
- 基于线性代数 + 贪心排序

**输出格式：**
- 文字列表，每行一个步骤
- 格式："Step 01, R 5 C 10"
- 包含 "Copy Steps" 按钮，一键复制所有步骤到剪贴板

### 7.2 创建 Android Shell

创建了 Android 应用壳：

**AndroidManifest.xml：**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cross100.solver">
    <application
        android:label="Cross Solver"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

**MainActivity.java：**
```java
package com.cross100.solver;
import android.app.Activity;
import android.os.Bundle;
import android.webkit.WebView;
import android.webkit.WebSettings;

public class MainActivity extends Activity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        WebView wv = new WebView(this);
        WebSettings ws = wv.getSettings();
        ws.setJavaScriptEnabled(true);
        ws.setDomStorageEnabled(true);
        setContentView(wv);
        wv.loadUrl("file:///android_asset/solver.html");
    }
}
```

---

## 第八阶段：APK 编译问题排查（2026年5月30日）

编译过程中遇到了 4 个问题，逐一排查解决。

### 8.1 问题1：android.jar 路径找不到

**错误信息：**
```
error: Unable to find package java.lang in platform classes
```

**根因分析：** 环境变量 `$ANDROID_HOME` 未设置，导致 aapt2 和 javac 找不到 android.jar。

**解决过程：**
```bash
find / -name "android.jar" 2>/dev/null
```
搜索结果：`/data/data/com.termux/files/home/android-sdk/platforms/android-34/android-34/android.jar`

**修复方法：** 在编译脚本中使用绝对路径引用 android.jar。

### 8.2 问题2：debug.keystore 路径找不到

**错误信息：**
```
FileNotFoundException: /data/data/com.termux/files/home/debug.keystore
```

**根因分析：** 签名用的 debug.keystore 文件不在预期的家目录下。

**解决过程：**
```bash
find / -name "debug.keystore" 2>/dev/null
```
搜索结果：`/data/data/com.termux/files/home/tmp/debug.keystore`

**修复方法：** 使用绝对路径 `/data/data/com.termux/files/home/tmp/debug.keystore` 进行签名。

### 8.3 问题3：APK 安装失败——"安装包异常"

**错误现象：** Android 系统安装 APK 时提示"安装包异常"，安装失败。

**根因分析：** DEX 文件（classes.dex）在 APK 内部的路径不对。`aapt add` 命令将 DEX 文件添加到了 `dex/classes.dex` 子目录中，而 Android 系统要求 `classes.dex` 必须在 APK 的根目录。

**原因：** 编译脚本中执行了：
```bash
aapt add solver.unsigned.apk dex/classes.dex
```
这会将文件以 `dex/classes.dex` 的路径添加到 APK 中。

**修复方法：**
```bash
cp dex/classes.dex .
aapt add solver.unsigned.apk classes.dex
```
先将 DEX 文件复制到当前目录，再以根路径添加到 APK。

### 8.4 问题4：APK 安装失败——"版本号 null"

**错误现象：** Android 系统安装 APK 时提示版本号为 null，安装失败。

**根因分析：** `aapt2 link` 命令没有正确注入 `versionCode` 和 `versionName`。虽然 AndroidManifest.xml 中声明了这些属性，但仅靠 XML 属性不够，还需要在 aapt2 命令行中显式指定。

**修复方法：** 在 aapt2 link 命令中添加版本参数：
```bash
aapt2 link --version-code 1 --version-name 1.0.0 ...
```

---

## 第九阶段：UI 与排序修复（2026年5月30日）

### 9.1 重复的主题切换按钮

**问题：** 求解器界面上有两个主题切换按钮——`#bt` 在 header 中，`#bt2` 在 footer 中。

**修复方法：** 移除了 `#bt2` 按钮及其 JavaScript 事件绑定，只保留 header 中的 `#bt` 按钮。

### 9.2 求解器步骤排序

**用户需求：** 求解器输出的步骤应按 R（行）、C（列）升序排列，格式如 "Step 01, R 5 C 10"。

**实现方案：**
1. 求解器计算出原始解法后，将所有步骤按 (r, c) 进行升序排序
2. 排序后进行模拟验证——确认排序后的步骤序列仍然能清除整个棋盘
3. 如果排序后的序列无效（理论上不会发生），回退到原始顺序

**代码逻辑：**
```javascript
// 排序
solution.sort((a, b) => a.r !== b.r ? a.r - b.r : a.c - b.c);

// 模拟验证
let valid = simulateAndCheck(grid, solution);
if (!valid) {
    solution = originalOrder; // 回退
}
```

---

## 附录 A：完整 Git 提交历史

| 提交哈希 | 类型 | 描述 | 标签 |
|----------|------|------|------|
| `38ec8a6` | feat | 初始 cross100 色彩消除益智游戏 | |
| `5f96a7e` | docs | 添加 README | |
| `5464955` | feat | 添加 Android WebView 壳 + 构建配置 | |
| `e15a389` | docs | 添加 CROSS 谜题逐步攻略（18步） | |
| `93d4307` | docs | 更新攻略：添加每步详细分析和策略说明 | |
| `b62cea5` | feat | i18n 中英文切换、修复胜利覆盖层 Bug、UI 打磨 | **v1.1.0** |
| `3ddf966` | docs | 更新攻略：Level 2 - 40步解法详细分析 | |
| `5460a30` | feat | 通用求解器——线性代数 + 贪心排序 | |
| `10c0b5e` | fix | 修正求解器——重叠感知线性代数模型 | **v1.2.0** |
| `27c3fa2` | docs | README 添加 APK 工具链版本和构建步骤 | |
| `28269b9` | docs | 添加完整 PROJECT_SUMMARY.md（16章，825行） | |
| `41624f3` | docs | 第二轮审查修正——求解器精度、死代码、覆盖层行为 | |
| `bb8ce5e` | docs | 第三轮修正——HTML 结构精度、覆盖层细节、按钮类 | |
| `865acb3` | docs | 第4–20轮审查——求解器代码精度、meta 标签归属、文件大小修正 | |
| `263b16a` | feat | 独立 Cross Solver APK——文字步骤输出、双主题、种子+导入 | |
| `8e54996` | fix | 求解器步骤按 R,C 升序排序并验证有效性 | |
| `05a2886` | docs | 完整问题与解决方案日志——项目全历史 | |

**共计 17 次提交，跨越整个项目生命周期。**

---

## 附录 B：关键设计决策汇总

| # | 决策点 | 选择 | 替代方案 | 原因 |
|---|--------|------|----------|------|
| 1 | 技术栈 | HTML5 + WebView | React Native / Flutter / 原生 | Termux 环境限制，无需重型工具链 |
| 2 | 胜利条件 | 最后点击清除恰好 19 个 | 清除所有即可 | 增加策略深度 |
| 3 | 关卡生成 | 堆叠法 | 纯随机 + 求解验证 | 保证每关有解，无需运行时求解 |
| 4 | 失败检测 | remaining < 19 | 遍历所有可能操作 | 简单高效，覆盖所有不可能情况 |
| 5 | CSS 方案 | JS 内联样式 | CSS 属性选择器 | WebView 兼容性 |
| 6 | 随机算法 | mulberry32 PRNG | Math.random() | 确定性可复现 |
| 7 | 旧关卡兼容 | 导入网格功能 | 种子搜索 | Math.random() 无法通过 PRNG 种子复现 |
| 8 | 检测时机 | 同步执行 | 异步回调 | 避免时序 Bug |
| 9 | 品牌名 | Cross100 | Prism2048 | 更准确反映游戏机制 |
| 10 | 求解器输出 | 文字列表 | 动画播放 | 用户明确需求 |
| 11 | 求解器步骤排序 | R,C 升序 | 原始顺序 | 用户可读性 |
| 12 | 语言切换 | i18n 对象 + data-i18n | 多 HTML 文件 | 单文件维护简单 |

---

## 附录 C：所有已修复 Bug 清单

| # | Bug 描述 | 影响 | 根因 | 修复方案 | 所在阶段 |
|---|----------|------|------|----------|----------|
| 1 | FAILED 覆盖层不显示 | 游戏无法检测失败状态 | `var try{...}` 非法语法导致整个 script 块解析失败 | 移除 `var` 关键字 | 第一阶段 |
| 2 | 方块不显示（白屏） | 棋盘完全空白 | WebView 不支持 CSS 属性选择器 `[data-color="1"]` | 改用 JS 内联样式 | 第一阶段 |
| 3 | Crack 播放期间误报失败 | 解法演示时弹出失败提示 | crack 期间 checkStuck 仍执行 | 添加 `!cracking` 条件 | 第一阶段 |
| 4 | 胜利画面不出现 | 通关后无祝贺弹窗 | 棋盘更新在 setTimeout 回调中，检测在同步中先执行 | 改为同步更新 | 第一阶段 |
| 5 | 冲击波位置偏移 | 点击特效位置不准确 | getBoundingClientRect 坐标需要校正 | 添加坐标修正计算 | 第一阶段 |
| 6 | Help 覆盖层无法关闭 | 帮助弹窗卡住 | 需要点击特定关闭按钮 | 改为点击任意位置关闭 | 第一阶段 |
| 7 | ◇/▶ 图标不对齐 | 按钮视觉不一致 | CSS 偏移 | 添加 offset 校正 | 第一阶段 |
| 8 | CSS 代码位置错误 | 样式不生效 | 样式放在了 JS 代码块中 | 移回 `<style>` 标签 | 第一阶段 |
| 9 | Crack 期间按钮可点击 | 状态冲突 | 未禁用按钮 | crack 期间禁用所有操作按钮 | 第一阶段 |
| 10 | 主题切换后方块颜色不更新 | 切换主题后棋盘颜色错误 | 未调用 render() | 主题切换后调用 render() | 第一阶段 |
| 11 | 按钮动作路由错误 | 胜利后点 NEW GAME 不起作用 | win/fail 按钮绑定的函数不对 | 胜利→newGame(), 失败→restartLevel() | 第一阶段 |
| 12 | stuckBanner 引号冲突 | 全页白屏 | innerHTML 中的单引号与 JS 字符串引号冲突，导致整个 script 块解析失败 | 修复引号转义 | 第一阶段 |
| 13 | v1.1.0 APK assets 过时 | 安装后只有基础功能 | 打包时使用了过时的 index.html | v1.2.0 完整同步 assets | 第五阶段 |
| 14 | Solver APK 安装——"安装包异常" | APK 无法安装 | DEX 文件在 APK 内的路径为 dex/classes.dex | 复制到根目录后重新添加 | 第八阶段 |
| 15 | Solver APK 安装——"版本号 null" | APK 无法安装 | aapt2 link 未注入版本号 | 添加 --version-code 和 --version-name 参数 | 第八阶段 |
| 16 | Solver APK 两个主题按钮 | UI 重复 | #bt 在 header，#bt2 在 footer | 移除 #bt2 及其事件绑定 | 第九阶段 |
| 17 | Solver 步骤未排序 | 输出顺序混乱 | 求解器输出的是原始操作顺序 | 按 R,C 升序排序 + 模拟验证 | 第九阶段 |

---

> 📝 **文档版本：** v1.0
> 📅 **最后更新：** 2026年5月30日
> 👤 **维护者：** AI Agent (Hermes)
